#include <bits/stdc++.h>
using namespace std;
void swap(int *a,int *b)
    {
       int t=*a;
       *a=*b;
       *b=t;
    }
void insertion_sort(int a[],int n)
{
    for(int i=1;i<n;i++)
     {
       for(int j=i-1;j>=0;j--)
           {
             if(a[j+1]<a[j])
              swap(&a[j],&a[j+1]);
           }
     
      }   

} 
int main()
{  int n;
	cin>>n;
int a[n];
for(int i=0;i<n;i++)
	cin>>a[i];
insertion_sort(a,n);
for(int i=0;i<n;i++)
  cout<<a[i]<<" ";

return 0;	
}

